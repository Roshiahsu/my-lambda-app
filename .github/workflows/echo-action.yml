name: Echo Action

on:
  push:
    branches:
      - yml-test

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: yml-test

      - name: Echo Message
        run: |
          echo "Test message from echo-action.yml"